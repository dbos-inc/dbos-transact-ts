{
  "name": "operon-hello",
  "version": "0.0.1",
  "scripts": {
    "build": "npx knex migrate:up && tsc",
    "test": "npx knex migrate:rollback && npx knex migrate:up && jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/supertest": "^2.0.14",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@dbos-inc/operon": "../..",
    "ts-node": "^10.9.1"
  }
}
