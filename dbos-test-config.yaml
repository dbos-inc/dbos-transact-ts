database:
  hostname: 'localhost'
  port: 5432
  username: 'postgres'
  password: ${PGPASSWORD}
  user_database: 'dbostest'
  user_dbclient: 'pg-node'
  connectionTimeoutMillis: 3000
telemetry:
  logs:
    logLevel: 'debug'
    addContextMetadata: true
    silent: true
application:
  testvalue: 'postgres'
